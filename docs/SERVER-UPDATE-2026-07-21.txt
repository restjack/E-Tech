SERVER UPDATE — 2026-07-21
Factorio game server mod sync (E-Tech 0.18.1 + patched SmartLinkedChests).
Server and clients must have EXACTLY these files or joins fail.

=== 1. UPLOAD THESE FILES (add or replace) ===

From "Factorio (E-Tech)\E-Tech\releases\":
  E-Tech_0.18.1.zip                        (skip 0.18.0 - superseded same day)

From "Factorio (E-Tech)\patched-mods\":
  Kux-SmartLinkedChests_3.3.7.zip          (SAME version number as before but
                                            DIFFERENT checksum - this is our
                                            E-Tech patched build. Server AND
                                            every client must replace their
                                            copy; version check will not catch
                                            it, joins fail on checksum until
                                            everyone runs this exact zip.)

=== 2. DELETE THESE FILES FROM THE SERVER MODS FOLDER ===

  E-Tech_0.17.x.zip / E-Tech_0.18.0.zip    (any older E-Tech zip)
  QualityEffectsFixed_1.1.2.zip            (RETIRED - absorbed into E-Tech
                                            0.18.0+ as the startup toggle
                                            "Quality adds module slots to all
                                            machines", on by default, now
                                            covers beacons/drills/labs too)

=== 3. CLIENTS ===

Players re-run install-mods.bat from the shared patched-mods folder
(SmartLinkedChests zip changed) and take the E-Tech update.
They should also DELETE QualityEffectsFixed_1.1.2.zip from their mods
folder (harmless if kept, but it does nothing anymore and one day its
portal updates could conflict).

=== 4. WHAT CHANGED IN THE PATCHED SMARTLINKEDCHESTS ===

Always-on (no settings needed):
  - Chest GUI: new "Pool" row (which chests this one can link with) and
    "Flow" display (net item flow of the pool, sampled 1/s while the GUI
    is open, ~15s window; red = draining, green = filling).
  - Chest GUI opens instantly (frame reused instead of rebuilt every open).
  - Tooltips on Link-id/Surface/Sector/Range rows; discard button tooltip fixed.
  - Expand Sector research tooltips now state the real values
    (+4/+8/+12 tiles per level) and note that /slc rescan applies them
    to existing chests.
  - /slc rescan: items that no longer fit after re-keying are spilled on
    the ground instead of silently deleted.

Optional behavior fix (per-save setting, default ON for new games only):
  - "[E-Tech] Experimental patches" (Map tab, runtime-global): global
    chests inside Factorissimo factories built on underground surfaces
    (Subsurface mod, Maraxis trench) link to the real planet.
    TO ACTIVATE ON THE EXISTING SERVER SAVE: load the save, enable the
    setting in Mod settings > Map (must be done in-save, not from the
    main menu), run /slc rescan, then SAVE. Main-menu edits only change
    defaults for new games; unsaved /c changes revert on reload.

=== 5. LESSON LOG (for future patches) ===

  - Display-only features: always-on, never behind runtime settings.
  - Runtime-global settings traps: main-menu edit = new-game default only;
    default_value changes never touch existing saves; /c changes need a
    manual save afterward.
  - New GUI elements in SLC: add via raw LuaGuiElement.add() on
    frame > content_flow > dataPanel - the mod's GuiBuilder DSL silently
    drops new declarative entries.
  - 2026-07-21: Nextcloud sync deleted data/elevated-rails + data/quality
    from the laptop test install mid-session (restored from Nextcloud web
    trash). Consider excluding Factorio/data and /bin from sync.
