# Build artifacts — zips rebuilt by build.ps1, archived in Nextcloud
E-Tech/releases/*.zip

# patched-mods/ and mods-replaced/ moved OUT of this repo on 2026-08-10:
#   Projects\Factorio (Patched-mods) 2.1.9\ — active patched mods, plus
#   Archived\ (retired builds + bookkeeping) and PATCH-NOTES.txt
# Neither is under git. This project is now only E-Tech and what ships to
# GitHub with it. absorbed-mods/ deliberately stayed and is still TRACKED —
# those are the originals of mods whose code was merged INTO E-Tech, so they
# are the licence paper trail rather than redistribution.

# OS / editor noise
Thumbs.db
Desktop.ini
*.swp

# Local convenience shortcuts
*.lnk

# Private working docs — session notes, local-machine inventories (stay in
# Nextcloud, never on GitHub)
E-TECH-HANDOFF.md
SESSION-RECAP-*.md
MOD-AUDIT-*.md
CODE-AUDIT-*.md
LOCAL-PATCHED-MODS.md

# Server upload staging folders - big mod zip bundles, distributed via Nextcloud
server-upload-*/
